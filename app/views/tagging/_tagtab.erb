<% if IssueTag.all.any? %>
<table class="list">
  <thead><tr>
  <th><%= l(:label_tagname) %></th>
  <th></th>
  </tr></thead>
  <tbody>
<% for issue_tag in IssueTag.all %>
  <tr class="<%= cycle 'odd', 'even' %>">
    <td><%=h(issue_tag.title) %></td>
    <td class="buttons">
      <%= link_to l(:button_edit), {:controller => 'issue_tags', :action => 'edit', :id => issue_tag.id }, :class => 'icon icon-edit' %>
      <%= link_to l(:button_delete), {:controller => 'issue_tags', :action => 'destroy', :id => issue_tag.id }, :method => :post, :class => 'icon icon-del' %>
    </td>
  </tr>
<% end %>
    </tbody>
</table>
<% else %>
<p class="nodata"><%= l(:label_no_data) %></p>
<% end %>
